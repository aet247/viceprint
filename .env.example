# Build-time (public, baked into client bundle)
PUBLIC_PAYPAL_CLIENT_ID=

# Server-side (Cloudflare secrets — NEVER commit real values)
PAYPAL_SECRET=
RESEND_API_KEY=
BUSINESS_EMAIL=
PAYPAL_ENV=sandbox
